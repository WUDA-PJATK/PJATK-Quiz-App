const getStaticProps = async () => {
    count res = await fetch('https://json  ')
}

export default function Creator() {
    
    return (
        <main>
            <h2>Tutaj będzie creator</h2>
        </main> 
    )
}